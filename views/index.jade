extends layout

block content
  h1= title
  p Este é o Sequelize para nós :)

  h2 Criar um novo utilizador
  form(action="/users/create", method="post")
    input(type="text", name="username")
    input(type="submit")

  h2 Utilizadores

  ul
  each user in users
    li
      = user.username
      | &nbsp;
      | (
      a(href="/users/" + user.id + "/destroy") Apagar
      | )
      ul
        li
          | Criar uma nova tarefa:
          form(action="/users/" + user.id + "/tasks/create", method="post", style="display: inline")
            input(type="text", name="title")
            input(type="submit")
        each task in user.Tasks
          li
            = task.title
            | &nbsp;
            | (
            a(href="/users/" + user.id + "/tasks/" + task.id + "/destroy") Apagar
            | )
